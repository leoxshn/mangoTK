rootProject.name = "mangoTK"

pluginManagement {
    repositories {
        mavenCentral()
        jcenter()
        gradlePluginPortal()
        maven { url = uri("https://dl.bintray.com/orangy/maven") }
        maven { url = uri("https://dl.bintray.com/kotlin/kotlinx") }
    }
}